{
  "name": "crawlerr",
  "version": "1.0.0",
  "description": "Chain crawler service",
  "main": "crawler.js",
  "type": "module",
  "bin": {
    "crawlerr": "dist/cli.js"
  },
  "scripts": {
    "test": "tap --no-coverage --reporter=specy -J --node-arg=--require=tsx",
    "build": "esbuild cli.ts services/Base.ts services/IoTexService.ts --platform=node --target=node16 --format=cjs --outdir=./dist"
  },
  "author": "hawyar",
  "dependencies": {
    "arg": "^5.0.2",
    "esbuild": "^0.14.47",
    "iotex-antenna": "^0.30.10",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/tap": "^15.0.7",
    "tap": "^16.3.0",
    "ts-standard": "^11.0.0",
    "tsx": "^3.6.0"
  },
  "ts-standard": {
    "ignore": [
      "bin",
      "dist"
    ]
  }
}
